pre-commit:
  parallel: true
  commands:
    lint:
      glob: 'src/**/*.{ts,tsx}'
      run: pnpm eslint {staged_files}
    typecheck:
      run: pnpm typecheck

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
