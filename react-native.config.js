/**
 * Autolink: point the example app to the library source at this repo root,
 * not the version published on npm.
 */
module.exports = {
  dependencies: {
    'react-native-accurate-location': {
      root: __dirname,
    },
  },
};
